import 'package:flutter/material.dart';

class LoginProvider with ChangeNotifier {
  // UserLogged? _userLogged;
  // UserModel? _userModel;

  // UserLogged? get userLogged => _userLogged;
  // UserModel? get userModel => _userModel;

  // set userLogged(UserLogged? user) {
  //   _userLogged = user;
  //   notifyListeners();
  // }

  // set userModel(UserModel? user) {
  //   _userModel = user;
  //   notifyListeners();
  // }

  // updateLogged(UserLogged? user) {
  //   userLogged = user;
  // }

  // updateUser(UserModel? user) {
  //   userModel = user;
  // }
}
